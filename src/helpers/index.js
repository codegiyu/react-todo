import formatDateTime from "./formatDateTime";
import { nextId, setTasks, resetTasks } from "./tasksManager";

const helpers = {
    formatDateTime,
    nextId,
    setTasks,
    resetTasks
}

export default helpers